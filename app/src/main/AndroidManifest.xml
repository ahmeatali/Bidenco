<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ahmetaliyilmaz.aay19.bidenco">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE">
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SaticiUrunEkleme"></activity>
        <activity android:name=".AliciEditProfileActivity" />
        <activity android:name=".AliciFeedActivity" />
        <activity android:name=".AliciProfileActivity" />
        <activity android:name=".SaticiEditProfileActivity" />
        <activity android:name=".SaticiProfileActivity" />
        <activity
            android:name=".ResetPasswordActivity"
            android:parentActivityName=".SigninActivity" />
        <activity android:name=".AliciSignupActivity" />
        <activity
            android:name=".SaticiSignupActivity"
            android:parentActivityName=".AliciSignupActivity" />
        <activity android:name=".SupportActivity" />
        <activity android:name=".SaticiFeedActivity" />
        <activity android:name=".SigninActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>